if ($argv[0]=="read");
        /* Check if the file exists */
        $err = fileinode($fn);
        if($err<0)>
                Guestbook is empty!<p>
                <?include "footer">
                </body></html>
                <?exit;
        endif;
